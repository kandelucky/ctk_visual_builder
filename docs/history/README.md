# Version history

Visual snapshots of CTkMaker across releases.

| Version | Screenshot | Highlights |
|---------|-----------|------------|
| **v1.0.5** | _no screenshot_ | **Group / Ungroup widgets.** New `group_id` field on `WidgetNode` ties a same-parent multi-selection together so the editor treats them as a unit while the model stays flat (no synthetic Frame, no nesting — generated Python is unaffected, `group_id` is skipped on export). Ctrl+G binds 2+ selected siblings (`Project.can_group_selection` rejects cross-parent + layout-container parents up front); Ctrl+Shift+G dissolves. Selection drill mirrors Frame's rhythm — cold click on a member targets the whole group, fast follow-up (within `_DRILL_WINDOW_MS = 800`) drills to a single member, slow re-click is a no-op so dragging a group repeatedly doesn't cycle the selection out from under the user. Drag always carries the whole group: `_build_group_starts` auto-expands the carried set to every member regardless of selection, and `_maybe_extract_from_container` routes through a new `_perform_group_extract` that pulls every member out together with relative offsets preserved (the original single-widget path was the source of the "only one comes out of the Frame" bug). Within-group multi-select is forbidden — `Project._enforce_group_invariant` (called inside `set_multi_selection`) collapses partial group selections to the primary so Ctrl/Shift+click on members can't end up with two but-not-all selected (whole-group selection still passes). Workspace draws an extra orange `#ff9f43` bbox around the union of members when the whole group is selected (separate frame pool in `selection_controller`, sits 4px outside the per-widget chrome edges). Object Tree shows a virtual `◆ Group (n)` parent row with members nested underneath (synthetic `group:<gid>` iid, expandable, soft orange foreground on the row text — background tint would override tk's selection highlight). Right-click context menu (canvas + tree) gains `Group` / `Ungroup` / `Select Group` (each gated by current state); virtual Group row right-click offers `Select Group` + `Ungroup`. Edit menu gains a `Group` / `Ungroup` pair plus an `Align ▶` submenu (mirrors the toolbar's 6 align + 2 distribute, state-refreshes alongside the buttons). `_drop_invalid_groups` runs at load time to silently strip cross-parent / layout-container-parented groups (data hygiene for externally-edited files). New `SetGroupCommand` for undo, integrates with the existing `BulkReparentCommand` for the multi-widget extract path. |
| **v1.0.4** | _no screenshot_ | **Marquee selection + smart snap guides + Window terminology cleanup.** Drag-rect on empty canvas now multi-selects (Photoshop / Figma touch mode — any overlap counts). Cyan dashed rect, 5px threshold so plain clicks fall through to the legacy "click empty → deselect". Shift+drag adds to existing selection. Works on both Select and Edit tools; Edit auto-flips to Select when the marquee picks up 2+ widgets (matches Ctrl+A's existing tool-switch). Top-level widgets only — children inside frames stay out of the result. **Smart snap guides:** while dragging a single place-managed widget, its edges / centre snap to siblings' and container's edges / centres within a 5px logical-pixel threshold. Cyan guide lines (matching the brand accent) appear at every matched reference and clear on release. Multi-widget drags skip snap (group cohesion wins). Hold Alt during drag to bypass snap. Implemented as `tk.Frame` widgets placed on the canvas (instead of `create_line`) so the guides float above document Frames — Tk canvas line items would otherwise hide behind window items. New pure-math `app/core/snap.py` (`compute_snap_offsets`) emits `(snap_dx, snap_dy, guide_xs, guide_ys)` tuples; drag controller applies the offsets to both the model and the visual canvas item per tick. **Window terminology cleanup:** menubar "Form" → "Window"; "Form Settings" → "Window Settings"; "All Forms" → "All Windows" (menu cascade + canvas dropdown); "Recent Forms" → "Recent Projects" (was misnamed — entries are recent .ctkproj projects, not forms inside one). Internal `Document` class / `documents` list / `_form_menu` var name left alone — refactor risk outweighs the consistency gain when those names never reach the user. README gains a "Project structure" section spelling out the four-level hierarchy: Project → Page → Window → Widget. Hero canvas screenshot refreshed for v1.0.3+. |
| **v1.0.3** | _no screenshot_ | **Alignment tools.** Eight new toolbar buttons: 6 alignment (Align Left / Center / Right + Align Top / Middle / Bottom) + 2 distribute (Distribute Horizontally / Distribute Vertically). Single button does the right thing based on selection: 1 widget selected → aligns to its container (Window / Frame / Card / etc.); 2+ widgets selected → aligns to the selection's bounding box (e.g. "Align Left" snaps every widget to the leftmost one's x). Distribute needs 3+ and equalizes gaps between consecutive bounding boxes (different widget sizes still distribute evenly visually). Buttons are disabled for layout-managed children (vbox / hbox / grid) — those parents own positioning, alignment doesn't apply. Mixed-parent selections also disable (no coherent coordinate space). New `app/core/alignment.py` (pure math, no Tk) emits move tuples for `BulkMoveCommand` so undo / redo rewinds the whole alignment as one history entry. Lucide icons: align-start-vertical / align-center-vertical / align-end-vertical (horizontal alignment), align-start-horizontal / align-center-horizontal / align-end-horizontal (vertical alignment), align-horizontal-distribute-center / align-vertical-distribute-center. |
| **v1.0.2** | _no screenshot_ | **Multi-page projects (Unity-style workspace).** A project is now a folder with `project.json` + `assets/pages/<page>.ctkproj` — one project holds multiple Page designs (Login, Dashboard, Settings, ...) sharing a single asset pool (fonts / images / icons). Inspired by Unity's scenes. Highlights: project.json schema (name, page list, font cascade) + walk-up asset resolver so deeply-nested page files still resolve `asset:images/foo.png` against the shared `assets/`. ProjectPanel double-click on `.ctkproj` switches page (Ctrl+O-style dirty prompt); right-click context menu (New / Duplicate / Rename / Delete page); active page bold + cyan with "(active)" badge. **File → Convert to Multi-Page Project** promotes legacy single-file `.ctkproj` into the folder layout (atomic rollback on failure, `.preconvert.bak` safety net). **Save As redesigned** with 3 scopes — Save Page As (in-project), Save Project As (full clone), Save Page to New Project (extracts the page + only its referenced assets into a fresh project). **Export dialog** gains All-pages + Page-X scope plus "Include only used assets" checkbox; per-page exports walk widget tokens + font cascade and bundle only the files that page touches. **Per-page autosave / .bak** moved to `<root>/.autosave/` + `.backups/` (id-keyed so renames don't orphan history); auto-migrates legacy sibling sidecars on first multi-page load. **External-deletion safety**: load-time prune of `project.json` drops missing entries and falls back to first surviving page when the active one vanished — friendly error for the "all pages gone" case. **Quick Export** (File → Export Active Document, chrome icon): one-question dialog (.py vs .zip with explainer body) → straight to `<project>/exports/<doc>.{py|zip}` with toast; asset filter scoped to that one document. **Recent projects** dedup by project root (page switches no longer pile up entries), drop missing files on add, dark-style context menu (Open / Open containing folder / Remove from Recent). Legacy single-file `.ctkproj` projects keep loading + saving unchanged — multi-page paths only kick in when `project.json` is present. |
| **v1.0.1** | _no screenshot_ | **Card widget + bug sweep.** New `Card` widget (renamed from internal `Shape`) with embedded image properties: anchor (9-point), tint, padding, preserve-aspect — drops into the palette with a `circle-stop` icon. Bug fixes: `_compute_drop_coords` was using `zoom.value` instead of `canvas_scale` so DPI-scaled drops landed off-target; hidden frames still acted as drop targets despite being invisible (now fall through cursor tests + drag-into-frame); Object Tree eye-icon cascade — children of an explicit-hidden parent now visually dim (use a third icon variant, not `eye-off` which is reserved for explicit hide); Save As only saved `.ctkproj` without copying `assets/` — now mirrors the asset tree to the new location. Lucide picker scroll resets on category switch. Card icon = `circle-stop`, Button icon updated to `square-mouse-pointer`. Backwards compat: existing `.ctkproj` files with `Shape` widgets auto-rename to `Card` on load. |
| **v1.0.0** | _no screenshot_ | **First stable release.** Visual canvas with real CTk widgets, drag-and-drop palette (19 widgets across CTkButton / CTkLabel / CTkFrame / CTkEntry / CTkProgressBar / CTkCheckBox / CTkRadioButton / CTkSwitch / CTkComboBox / CTkOptionMenu / CTkSlider / CTkTextbox / CTkSegmentedButton / CTkTabview / CTkScrollableFrame plus builder-only Image / Vertical Layout / Horizontal Layout / Grid Layout). Multi-document workspace (Main + Dialogs in one project). Layout managers (place / vbox / hbox / grid) with WYSIWYG canvas. Asset system (fonts / images / 1700+ Lucide icons) with portable `asset:` tokens. Clean Python code export. Properties panel v2 with rich editors (drag-scrub, color swatches with eyedropper, scrollable dropdowns, font picker). Full undo/redo with History panel. ZIP export bundle. PyPI placeholder package `ctkmaker` + GitHub release. |
| **v0.0.15.9** | _no screenshot_ | **Inspector UX polish — clearable colour properties, drag-scrub + menu bug fixes, Image descriptor cleanup.** New schema field ``clearable`` on colour properties (accepts bool OR a callable that inspects the current node's props for context-dependent cases); when set, the Color editor draws a small ✕ button at the right edge of the value cell that resets the prop to its ``clear_value`` (``None`` for Image tint / Button image tint, ``"transparent"`` for fg_color). ✕ glyph dims to the bool-off grey when the value is already at the sentinel so layout stays stable. ``CTkFrame`` / ``CTkScrollableFrame`` use the callable form so ``fg_color`` is only clearable on Layout Frames (``layout_type`` in vbox / hbox / grid) — plain ``place`` Frames keep their fill intact. ``CTkButton`` ``fg_color`` + ``image_color`` + ``image_color_disabled`` are always clearable (icon-only buttons want a transparent fill). ``Image`` descriptor gains clearable on ``image_color`` + ``fg_color``, and ``image_color_disabled`` is dropped entirely — ``CTkLabel`` has no disabled state so the ``_disabled`` tint property was always dead on the Image widget, a copy-paste leftover from the CTkButton schema. "transparent" is displayed as **"none"** in the Inspector value column (the stored value stays "transparent" for CTk runtime compatibility) — builder preview doesn't render true transparency, so "none" communicates intent better. Bug fixes: (1) Object Tree right-click context menu had Windows tk.Menu's native emboss/shadow effect on ``state="disabled"`` entries (Paste when clipboard empty, Paste as child on leaves); switched to the same pattern the top Edit menu already uses — keep every item at ``state="normal"`` and swap foreground colour only, callbacks no-op when the action can't run. Also reparented the menu from a CTkFrame child to ``winfo_toplevel()`` which fixed the non-disabled items' emboss. Menu order settled as Copy / Paste / Paste as child / Duplicate / Rename / Delete / Bring to Front / Send to Back. (2) Drag-scrub ``<->`` cursor vanished after release when the mouse was still hovering the scrub target — ``_on_release`` cleared the cursor but left ``_cursor_mode`` stuck at ``"drag"``, so the next ``<Motion>`` short-circuited on ``new_mode == self._cursor_mode``. Now reset ``_cursor_mode = ""`` at release time too so the next hover re-applies the cursor. |
| **v0.0.15.8** | _no screenshot_ | **Layout module refactor pass + dead-code sweep.** `apply_child_manager` (150 lines, three branches stuffed with grow equal-split + composite size + before-anchor logic) split into a 25-line dispatcher plus four focused per-manager methods: `_apply_pack_manager` / `_apply_grow_equal_split` / `_apply_grid_manager` / `_apply_place_manager`. Composite widget sizing — repeated `if is_composite and lw > 0 and lh > 0: configure(width=, height=)` across `apply_child_manager` (pack + place branches), `_place_nested` (pack + place branches), and `_place_top_level` — collapsed into two helpers (`_composite_configure` / `_composite_place_size`) so all five sites are one-liners. `_grid_child_place_kwargs` sticky logic deduplicated via `_sticky_axis(has_lo, has_hi, avail_pos, avail_size, child_size)` — same table for both axes, called twice instead of two near-identical 5-branch if/elif blocks. Object Tree dead methods removed: `_paste_at_tree_row` (replaced by `_paste_in_window` + `_paste_as_child`) and `_duplicate_with_history` (replaced by `_duplicate_in_window`). Stale `axis_key`/`prop_key` duplicate variable in `_apply_grow_equal_split` consolidated. Module docstring of `layout_overlay.py` updated to list the new helpers. No behaviour change — pure structural cleanup, every Area 2 scenario continues to pass. |
| **v0.0.15.7** | _no screenshot_ | **Area 2 — Layout managers QA pass complete (44 / 44) + 7 bug fixes + auto-fill defaults + numeric spinner + grow equal-split.** Layout testing turned up seven discrete fixes: L2-1 selection chrome z-order regression (chrome dropped behind widgets after the v0.0.15.6 frame-pool refactor — `_position_edges` / `_position_handles` / `_position_outline` now `frame.lift()` on every show); L2-2 grid auto-grow undo orphan (palette drop / drag-reparent / duplicate / paste into a full grid grew the container's `grid_rows`/`grid_cols` via direct `update_property` calls outside any command — `AddWidgetCommand` / `ReparentCommand` / `MultiChangePropertyCommand` / `BulkAddCommand` gained an optional `parent_dim_changes=(container_id, {prop: (before, after)})` field, `_auto_assign_grid_cell` stashes the change on the node as `_pending_parent_dim_changes`, each call site harvests it and threads it into the command); L2-3 dead `_grid_cell_at` reference in `_on_palette_drop` (latent rename leftover, swapped to `_grid_indicator.cell_at`); L2-4 cascade-delete undo restored only the root (`_restore_widget` called `add_widget` once for the root snapshot, so descendants stayed in the model with no view — new `_add_subtree_recursive` helper detaches children + recurses, mirroring `_paste_recursive` so `widget_added` fires per node); L2-5 container duplicate dropped every child (`Project.duplicate_widget` cloned the root only — switched to `_clone_with_fresh_ids(node.to_dict())` + `_paste_recursive` deep-clone, plus an optional `force_top_level` for the Tree's "Duplicate in window"); L2-6 layout-in-layout nesting blocked on drag but allowed on paste (`paste_from_clipboard` now refuses to nest a layout container inside another layout container, falls through to top-level); L2-7 right-click skipped drill-down (`_on_widget_right_click` now routes through `drag_controller._resolve_click_target` so right-click matches left-click drill behaviour). New features: **auto-fill defaults** — `WidgetDescriptor.prefers_fill_in_layout` flag (True for Button / Label / Entry / Textbox / Frame / ScrollableFrame / ProgressBar / Slider / SegmentedButton / Tabview / Image; False for CheckBox / RadioButton / Switch / OptionMenu / ComboBox); fresh drops of fill-friendly widgets into vbox / hbox commit `stretch="grow"` instead of `"fixed"`, into grid commit `grid_sticky="nsew"`. Layout-type swap re-applies fill defaults to existing children so Grid → vbox stops leaving widgets at their prior `fixed` (`_handle_container_layout_prop` calls `lifecycle.apply_fill_defaults_to_children` on `layout_type` changes only). **Grow equal-split** — pack manager with `stretch="grow"` now explicitly sizes each grow child to `(container - spacing × (N-1)) / N` along the main axis (height for vbox, width for hbox) so swapping Grid → vbox actually shrinks oversized cell-configured widgets to fit. **Inspector ±1 spinner** — every numeric row gets a tiny vertical ▲/▼ spinner overlay pinned to the right edge of the value cell (Qt Designer-style); click steps ±1, hold auto-repeats (350ms initial delay, 60ms tick); press / release brackets one `ChangePropertyCommand` so a hold collapses into a single undo step; same min/max clamp as horizontal drag-scrub. **Object Tree paste menu** — replaced single "Paste" with "Paste" (window root), "Paste as child" (container only, disabled on leaves), "Duplicate" (always lands at window root, separate from canvas's in-place duplicate). **Layout type dropdown** dropped the `place` option from container Inspector — once a container commits to a managed layout it can't fall back to absolute positioning. |
| **v0.0.15.6** | _no screenshot_ | **Workspace refactor round complete — chrome / widget_lifecycle / render / selection_controller / zoom_controller.** Chrome strip gets per-doc `chrome_doc:{id}` umbrella tag — `drive_drag` now slides chrome + doc rect + grid via three `canvas.move` calls instead of the per-motion `_redraw_document` that deleted + recreated ~8 items × N docs per tick. Per-button x-offset constants (`SETTINGS_X_OFFSET` / `TOFRONT_X_OFFSET` / …), `_draw_icon_button` + `_bind_icon_hover` helpers, and a cursor-sticking fix on tofront/toback hover leave. Settings hover bug — was swapping every doc's gear icon at once because the handler used the shared `CHROME_SETTINGS_IMG_TAG` instead of the per-doc tag. `widget_lifecycle.on_widget_added` (120 lines) split into a 55-line orchestrator + 4 helpers (`_resolve_master` / `_instantiate_widget` / `_disable_container_propagate` / `_place_top_level` / `_place_nested`). `render.update_visibility_across_docs` (87 lines, 2 near-duplicate loops) split into a 20-line orchestrator + 4 helpers (`_is_doc_drag_active` / `_compute_doc_bboxes` / `_update_visibility_for_document` / `_should_hide_widget`). `selection_controller` gets the session's biggest perf win — `_edges` (4) + `_handles` (8) + `_outline_pool` frames are allocated once and REUSED across every `draw` / `clear` cycle via `state="hidden"` / `"normal"`, so a stable 20-widget multi-selection costs ~3ms (canvas.coords + itemconfigure) instead of ~45ms (88 tk.Frame destroy+create per draw). `_edge_geoms` extracted as the single source of truth for 4 edge rectangles (previously inlined in 3 sites). `_bbox_for` no longer calls `update_idletasks()` per widget — the public entry points (`draw` / `update`) flush once at the top, saving N-1 redundant flushes per multi-select draw. `zoom_controller.apply_to_widget` gets a `winfo_exists()` guard so the per-call try/except blocks stop swallowing genuine option / geometry errors as "widget gone". Bug fixes: WS-34 (ScrollableFrame `grid_propagate(False)` raised TypeError on load — catch alongside TclError), WS-35 (saving un-hid every hidden widget because `update_visibility_across_docs` wrote `state="normal"` unconditionally — now respects `node.visible`), WS-36 (unhiding a grid child landed it at cell 0,0 — `_set_widget_visibility` now dispatches on parent layout_type and routes grid children through `apply_child_manager`). Doc drag also fixed to call `selection.draw()` (not `update()`) so multi-select outlines follow the document to its new position. |
| **v0.0.15.5** | _no screenshot_ | **Drag perf + layout nesting block + Edit button + locked UX.** Canvas drag motion now hit-tests a press-time cache of grid containers instead of walking every widget — projects with no grid Frames pay zero per-motion cost, projects with a few pay O(grid_count). Layout containers (Vertical / Horizontal / Grid Layout) can no longer be nested inside each other at drop time — palette drop + drag reparent reroute layout-into-layout drops to top-level (WS-33; true nesting deferred to a dedicated mini-phase because canvas rendering of nested grids is fragile). Properties panel gains an "Edit" pencil button in the Name row while Select tool is active: one click narrows a multi-selection to the primary widget and flips the workspace tool to Edit, so users don't have to reach for the toolbar to start editing. Locked widgets are now canvas-inert — clicks on a locked widget (or anywhere in its subtree, since `_effective_locked` cascades to descendants) are silently ignored; the Object Tree is the only surface that can touch them, matching Figma / Photoshop / Illustrator conventions. |
| **v0.0.15.4** | _no screenshot_ | **drag.py refactor + ghost perf + cross-doc group undo.** Drag controller decomposed: `GridDropIndicator` lifted to its own 154-line module; `on_release` (177 → 45-line orchestrator) split into `_teardown_hidden_mode` / `_should_snap_back` / `_apply_snap_back` / `_record_bulk_move`; `_maybe_reparent_dragged` (200 → 70-line orchestrator) split into `_compute_drop_coords` / `_perform_cross_doc_group_move` / `_push_reparent_command`; `on_press` (166 → 65-line orchestrator) split into `_resolve_selection` / `_build_group_starts` / `_tag_drag_group`; `on_motion` (132 → 45-line orchestrator) split into `_motion_feedback` / `_motion_place_group`. Drag ghost for pack/grid children swapped from a per-gesture `tk.Toplevel` (native OS window) to two canvas items (rect + text) tagged `drag_ghost` — each motion is now one `canvas.move` call instead of a Toplevel geometry update. Bug fix: `BulkReparentCommand` added so cross-document group drag records one history entry per moved widget; previously only the primary widget came back on undo and the rest stayed stranded in the destination doc (WS-32). |
| **v0.0.15.3** | _no screenshot_ | **Large-group drag perf + 2 regression fixes.** Group drags of 10+ widgets enter "ghost mode": the widgets + their selection chromes flip to `state="hidden"` on press, a single dashed rectangle tracks the cursor, and `apply_all` runs once on release to drop them at the final position — per-motion tk cost collapses from O(N configure + N font rebuild) to 1 rect move. Document (chrome title bar) drags of 10+ widgets use the same trick: widgets hide, only the doc outline + title bar follow the cursor, then `apply_all` resyncs on release. Bug fixes this pass: (1) `renderer.update_visibility_across_docs` was flipping our intentionally-hidden widgets back to `state="normal"` every motion — now respects a `workspace._doc_drag_hide_active` flag; (2) cross-document widget drag was tripping the off-form snap-back guard because bounds were compared against the *source* doc only — now tests the drop point against every doc's canvas rect. See `docs/testing/workspace.md` WS-30, WS-31. |
| **v0.0.15** | _no screenshot_ | **Area 1 — Workspace core QA pass, 22 bugs fixed.** Cross-document undo/redo now carries `document_id` through `AddWidgetCommand` / `BulkAddCommand` / `DeleteWidgetCommand` / `DeleteMultipleCommand` / `ReparentCommand` so palette drops, paste, duplicate, delete, and drag-reparent all restore into the original doc instead of whichever is active at replay time. `project.add_widget` + `project.reparent` gain a `document_id` param; `project.paste_from_clipboard` gains `base_position` for cursor-anchored paste. Canvas right-click on empty area opens Paste (at cursor) / Select All / Deselect All; right-click on a widget gains Copy / Paste / Duplicate above Rename / Delete / Z-order; multi-select members get `Copy N` / `Duplicate N` / `Delete N` without collapsing the selection. Select All auto-switches Edit→Select tool. Keyboard Delete routes multi-selection through `DeleteMultipleCommand` (locked-guard blocks the whole batch). Tree drag reparent cascade-resets x/y in the new parent and is now fully undoable. Accent colors switch from UUID-hash to doc-index × golden-ratio so two forms can't land on visually-close hues. Left-click on the empty area of a non-active document activates it. Document drag undo/redo re-places widgets via a new `document_position_changed` event. Window Settings opens from the gear icon in Select tool (Hand tool stays strict). See `docs/testing/workspace.md` WS-1..WS-22 for the full finding log. |
| **v0.0.14** | [![v0.0.14](v0.0.14.png)](v0.0.14.png) | **Phase 6.6 — Grid place-based centring + workspace refactor.** CTkFrame's internal canvas (rounded corners + border) broke tk's native `.grid()` centring math — children landed at ¾ of the cell's vertical midpoint. Canvas preview switches to hand-computed `.place()` coordinates that handle every sticky combination (empty = centered, `nsew` = fill, axis-fill, edge-align). Runtime export adds `uniform="col"` / `uniform="row"` on `grid_*configure` so empty cells stay equal-sized (weight=1 alone only splits *extra* space). `Window` loses `layout_type` (absolute positioning only). `WidgetLifecycle` class extracted from `workspace/core.py` (1342 → 998 lines, -26%), handling `widget_added` / `_removed` / `_reparented` / `_z_changed` / `_visibility_changed` / `_locked_changed`. Dead code swept: `_child_grid_kwargs`, `_configure_grid_weights`, `iter_containers` removed. |
| **v0.0.13** | _no screenshot_ | **Phase 6.5 — Grid WYSIWYG + drag-to-cell + runtime parity.** Grid Frames render children into real cells on canvas via tk `.grid()`; user pins grid size via `grid_rows` / `grid_cols` (default 2×2, authoritative — no auto-grow). Drag snaps to the cell under the cursor with a light-blue dashed outline during the gesture; palette drops land at the cursor cell; fresh widgets auto-assign to the next free cell (row-major scan). Runtime export emits `pack_propagate(False)` + `grid_propagate(False)` + `grid_rowconfigure(weight=1)` + `grid_columnconfigure(weight=1)` so runtime matches canvas. `hidden_when` predicate added to the Properties panel — the Dimensions row vanishes on non-grid containers instead of greying out. `project.reorder_child_at(widget_id, final_index)` API for clean drag-to-cell reorders. "Group" palette preset retired (plain Frame + Border toggle does the same). Canvas `[vbox]` / `[hbox]` / `[grid]` badges retired. |
| **v0.0.12** | _no screenshot_ | **Phase 6.4 — Stage 3.1 WYSIWYG + Layout presets + workspace split.** vbox / hbox Frames now render children with real `pack()` on the canvas — the gap between builder preview and exported runtime is closed for the first time. Palette gains a `Layouts` category with four Qt Designer-style presets (`Vertical Layout` / `Horizontal Layout` / `Grid Layout` / `Group`). Child layout collapses from 4 pack_* fields to a single `stretch` enum (`fixed` / `fill` / `grow`), containers gain a `layout_spacing` knob, and `pack_propagate(False)` keeps Frames from shrinking to their children. The 2372-line `workspace.py` also split into a 6-file package (`core` + `chrome` + `controls` + `drag` + `layout_overlay` + `render`), with `core.py` down 48%. Load migration for v0.0.11 projects: legacy `pack_fill` / `pack_expand` translate to the matching `stretch` value. |
| **v0.0.11** | _no screenshot_ | **Phase 6.3 — pack split into vbox / hbox + layout icons.** Matches Qt Designer's 20-year-old UX: `pack` was ergonomically ambiguous so it's been split into `Vertical Layout` (tk `side="top"`) and `Horizontal Layout` (tk `side="left"`). Children drop the `pack_side` row entirely — direction now lives on the parent. Properties panel dropdown renders each option with its Lucide icon (`crosshair` / `rows-3` / `columns-3` / `grid-3x3`); canvas chrome, Object Tree + container badges all updated to the new labels. Legacy v0.0.10 `.ctkproj` files auto-migrate on load (`pack` → `vbox`, or `hbox` if any child had `pack_side=left/right`). Plus: Widget Box palette gained a collapse toggle (icon-only strip when folded). |
| **v0.0.10** | _no screenshot_ | **Phase 6 — Layout managers (stage 1 + 2).** Containers (Window, Frame, ScrollableFrame) gain `layout_type` ∈ `place / pack / grid`; child widgets pick up parent-driven `pack_*` / `grid_*` rows in the Properties panel; code exporter swaps `.place()` for `.pack()` / `.grid()` per parent; canvas shows the choice via chrome-title suffix (`· pack` / `· grid`), `[pack]` / `[grid]` badges in container corners, and dashed grid-cell overlays; Object Tree marks containers with the same suffix. Drag/drop on the canvas is still absolute — true WYSIWYG arrangement is stage 3. |
| **v0.0.9** | [![v0.0.9](v0.0.9.png)](v0.0.9.png) | **Phase 5.5 — Multi-document canvas.** One `.ctkproj` holds a Main Window (`ctk.CTk`) plus any number of Dialogs (`ctk.CTkToplevel`), all visible on the same canvas. Per-document chrome with drag-to-move, active highlight, close ✕, Image widget, palette drop targeting the doc under cursor, cross-document widget drag, smart widget masking for overlapping forms, Object Tree focused on active document with bottom status strip, Window Settings panel (title, size, fg_color live preview, resizable, frameless, builder grid style/colour/spacing), AddDialog preset picker (Alert / Compact / Medium / Settings / Wizard / Same as Main), code exporter emits one `.py` with N classes. |
| **v0.0.8** | [![v0.0.8](v0.0.8.png)](v0.0.8.png) | Phase 3 widgets (13/15 — Entry, CheckBox, ComboBox, OptionMenu, ProgressBar, RadioButton, SegmentedButton, Slider, Switch, Textbox, Tabview, ScrollableFrame). Full Undo/Redo with History panel (F9), Edit menu, coalesced nudges + property scrubs, Reparent / Paste / Duplicate / Z-order / Visibility / Lock toggles, inline rename coalescing. Preview button, Georgian keyboard fallback. |
| **v0.0.7** | [![v0.0.7](v0.0.7.png)](v0.0.7.png) | Properties panel v2 rewrite — ttk.Treeview backbone, flicker-free overlays, modular editor registry. |
| **v0.0.6** | [![v0.0.6](v0.0.6.png)](v0.0.6.png) | CTkLabel / CTkFrame, workspace canvas, startup dialog. |
