"""Pure-Python runtime widget classes — no dependency on CTkMaker.

Modules in this package contain widget implementations that the
exporter inlines verbatim into generated `.py` files. They are also
imported by descriptors so the builder canvas renders the same
widgets the user will see at runtime.
"""
